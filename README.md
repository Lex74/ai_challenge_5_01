# Telegram Bot - Личный коуч с использованием OpenAI API

Telegram бот на Python, который помогает пользователям ставить цели по фреймворку SMART с использованием OpenAI API.

## Возможности

- 🤝 Помощь в постановке целей по фреймворку SMART
- 💬 Интеллектуальный диалог с пользователем
- 🧠 Долгосрочная память с автоматической саммаризацией
- ⚙️ Настройка системного промпта, температуры, модели и других параметров
- 📊 Отслеживание стоимости запросов к OpenAI API
- 💾 Сохранение контекста диалога на диск
- 📚 **RAG (Retrieval-Augmented Generation)** - поиск релевантных чанков из документов и использование их в ответах
- 🔍 **Сравнение ответов** с RAG и без RAG для анализа эффективности

## Требования

- Python 3.8 или выше
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- OpenAI API Key (получить на [platform.openai.com](https://platform.openai.com/api-keys))
- (Опционально) Admin User ID для получения логов
- (Опционально) Node.js и npm для работы с MCP сервером Notion (для команды `/notion_tools`)

## Установка

1. Клонируйте репозиторий или скачайте файлы проекта

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` в корне проекта:
```bash
touch .env
```

4. Отредактируйте файл `.env` и добавьте ваши токены:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
OPENAI_API_KEY=ваш_ключ_api
ADMIN_USER_ID=ваш_telegram_id  # Опционально, для получения логов

# MCP сервер Notion (опционально, для команды /notion_tools)
MCP_NOTION_COMMAND=npx -y @notionhq/notion-mcp-server
MCP_NOTION_ARGS=
```

## Получение токенов

### Telegram Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в файл `.env`

### OpenAI API Key

1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com)
2. Перейдите в раздел [API Keys](https://platform.openai.com/api-keys)
3. Создайте новый API ключ
4. Скопируйте ключ в файл `.env`

### Admin User ID (опционально)

1. Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Скопируйте ваш ID в файл `.env`

### Настройка MCP сервера Notion (опционально, для команды `/notion_tools`)

1. Установите Node.js версии 18 или выше (если еще не установлен):
   
   **Если возникли проблемы с установкой через NodeSource**, попробуйте следующие шаги:
   
   - **Шаг 1: Очистка и исправление** (если была ошибка dpkg):
     ```bash
     sudo apt-get clean
     sudo apt-get update
     sudo dpkg --configure -a
     sudo apt-get install -f
     ```
   
   - **Шаг 2: Установка через nvm** (рекомендуется, если NodeSource не работает):
     ```bash
     curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
     source ~/.bashrc  # или перезапустите терминал
     nvm install 20
     nvm use 20
     nvm alias default 20
     ```
   
   - **Или через NodeSource** (если nvm недоступен):
     ```bash
     curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
     sudo apt-get install -y nodejs
     ```
   
   - **macOS**: `brew install node`
   - **Windows**: Скачайте LTS версию с [nodejs.org](https://nodejs.org/)
   
   **Важно**: Требуется Node.js версии 18 или выше. Проверьте версию: `node --version`

2. MCP сервер Notion запускается автоматически через `npx` при выполнении команды `/notion_tools`.
   Используется официальный MCP сервер Notion через `mcp-remote` (загружается автоматически).
   
   **Важно для VPS без браузера**: При первом запуске потребуется авторизация OAuth.
   
   **Способ 1: SSH туннель (рекомендуется)**
   - На локальной машине создайте туннель:
     ```bash
     ssh -L 9553:localhost:9553 user@your-vps
     ```
   - Скопируйте URL авторизации из логов бота
   - Откройте его в браузере на локальной машине
   
   **Способ 2: Текстовый браузер (если установлен lynx/links)**
   - Скопируйте полный URL из логов (включая все параметры)
   - Запустите с кавычками:
     ```bash
     lynx "https://mcp.notion.com/authorize?response_type=code&client_id=...&..."
     ```
   - **Важно**: Весь URL должен быть в кавычках, иначе shell разобьет команду
   
   После авторизации токен сохранится и повторная авторизация не потребуется

3. (Опционально) Настройте команду в `.env` файле:
   ```env
   MCP_NOTION_COMMAND=npx
   MCP_NOTION_ARGS=-y mcp-remote https://mcp.notion.com/mcp
   ```
   
   Подробнее: [Документация Notion MCP](https://developers.notion.com/docs/get-started-with-mcp)

## Запуск

Запустите бота командой:
```bash
python bot.py
```

Бот начнет работать и будет отвечать на сообщения в Telegram.

## Использование

1. Найдите вашего бота в Telegram (по имени, которое вы указали при создании)
2. Отправьте команду `/start` для начала работы
3. Расскажите боту о своей цели, и он задаст вопросы для формулировки цели по SMART

### Команды бота

#### Основные команды
- `/start` - начать работу с ботом (очищает историю диалога)
- `/help [вопрос]` - показать справку по всем командам или ответить на вопрос о проекте (ассистент разработчика с RAG и Git MCP)

#### Управление системным промптом
- `/setprompt <промпт>` - установить новый системный промпт
- `/getprompt` - показать текущий системный промпт
- `/resetprompt` - сбросить системный промпт к дефолтному

#### Управление температурой
- `/settemp <температура>` - установить температуру запроса (0.0-2.0)
- `/gettemp` - показать текущую температуру
- `/resettemp` - сбросить температуру к дефолтной (0.2)

#### Управление моделью
- `/setmodel <модель>` - установить модель OpenAI (например: gpt-4o-mini, gpt-4o, gpt-3.5-turbo)
- `/getmodel` - показать текущую модель
- `/resetmodel` - сбросить модель к дефолтной (gpt-4o-mini)

#### Управление токенами
- `/setmaxtokens <количество>` - установить максимальное количество токенов
- `/getmaxtokens` - показать текущее максимальное количество токенов
- `/resetmaxtokens` - сбросить к дефолтному значению (1000)

#### MCP сервер Notion
- `/notion_tools` - показать список доступных инструментов Notion из MCP сервера (требует Node.js и npm)

#### MCP сервер Kinopoisk
- `/kinopoisk_tools` - показать список доступных инструментов Kinopoisk MCP (требует Python и `kinopoisk-mcp/mcp_server.py`)
- `/kp_search <ключевое_слово> [страница]` - поиск фильмов на Кинопоиске по ключевому слову через MCP

#### MCP сервер Git
- Git MCP сервер автоматически интегрирован в ассистента разработчика
- Доступные инструменты через `/assistant`:
  - Получение текущей ветки git
  - Статус git репозитория (измененные файлы)
  - Список открытых/измененных файлов
  - Содержимое файлов из репозитория
  - Последние коммиты
  - Diff (различия) файлов

#### RAG (Retrieval-Augmented Generation)
- `/rag_mode <режим>` - установить режим RAG:
  - `off` - без RAG (обычный режим, по умолчанию)
  - `on` - с RAG (используется контекст из документов)
  - `compare` - сравнение ответов с RAG и без RAG
  - `compare_filter` - сравнение ответов с фильтром релевантности и без фильтра
- `/getragmode` - показать текущий режим RAG
- `/setragthreshold <порог>` - установить порог релевантности для фильтрации (0.0-1.0)
- `/getragthreshold` - показать текущий порог релевантности
- `/setragrerank <метод>` - установить метод реранкинга:
  - `similarity` - сортировка по релевантности (по умолчанию)
  - `diversity` - убирает дубликаты, оставляет уникальные чанки
  - `hybrid` - комбинация similarity и diversity
  - `off` - отключить реранкинг

#### Ассистент разработчика (через /help)
- `/help [вопрос]` - ассистент разработчика с доступом к:
  - Документации проекта через RAG (README, API, схемы данных)
  - Git репозиторию через MCP (ветки, файлы, статус, diff, коммиты)
  - Коду проекта через чтение файлов
  
  Примеры использования:
  - `/help как работает RAG в этом проекте?`
  - `/help покажи текущую ветку git`
  - `/help какие файлы изменены?`
  - `/help покажи содержимое файла bot.py`
  - `/help объясни структуру проекта`

**Как работает RAG:**
1. При получении вопроса бот ищет релевантные чанки в индексе документов
2. **Фильтрация по порогу релевантности** (если включена) - отсеивает нерелевантные чанки
3. **Реранкинг результатов** (если включен) - улучшает порядок чанков
4. Найденные чанки объединяются с вопросом
5. LLM получает вопрос с контекстом из документов
6. В режиме `compare` вы получаете оба ответа (с RAG и без) и анализ сравнения
7. В режиме `compare_filter` вы получаете оба ответа (с фильтром и без) и анализ эффективности фильтрации

**Фильтрация и реранкинг:**
- **Порог релевантности** (0.0-1.0): отсеивает чанки с низким косинусным сходством
  - Рекомендуемые значения: 0.2-0.5
  - Чем выше порог, тем строже фильтрация
- **Реранкинг**: улучшает качество результатов поиска
  - `diversity` - убирает дубликаты, увеличивает разнообразие
  - `hybrid` - комбинация релевантности и разнообразия

**Требования для RAG:**
- Индекс документов должен быть создан (файл `document_index/index.json`)
- Для создания индекса используйте:
  - `index_documents.py` - для индексации произвольных документов
  - `index_project_docs.py` - для индексации документации проекта (README, API, схемы данных)
- Для получения эмбеддингов требуется OLLama или OpenAI API

**Индексация документации проекта:**
```bash
# Используя OLLama (по умолчанию)
python index_project_docs.py

# Используя OpenAI
USE_OPENAI_EMBEDDINGS=true python index_project_docs.py
```

После индексации документации ассистент разработчика (`/help [вопрос]`) сможет отвечать на вопросы о проекте, используя информацию из документации.

#### Сброс диалога
- Отправьте сообщение "стоп" или "стой" для сброса истории диалога

## Синхронизация на сервер

Для синхронизации проекта на удаленный сервер используйте скрипт `sync.sh`:

```bash
./sync.sh
```

Скрипт автоматически:
- Синхронизирует файлы на сервер через rsync
- Исключает ненужные файлы (`.env`, `memory/`, `__pycache__/` и т.д.)
- Перезапускает сервис `telegram-bot.service` на сервере

Параметры синхронизации настраиваются в файле `.env`:
- `SYNC_LOCAL_DIR` - локальная директория проекта
- `SYNC_REMOTE_USER` - пользователь на удаленном сервере
- `SYNC_REMOTE_HOST` - адрес удаленного сервера
- `SYNC_REMOTE_DIR` - директория на удаленном сервере

Если переменные не заданы, используются значения по умолчанию.

## Структура проекта

```
.
├── bot.py                 # Основной файл запуска бота
├── config.py              # Конфигурация и загрузка переменных окружения
├── constants.py           # Константы и настройки (промпты, модели, цены)
├── memory.py              # Работа с памятью пользователей (загрузка/сохранение)
├── openai_client.py       # Работа с OpenAI API (запросы, саммаризация, расчет стоимости)
├── utils.py               # Утилиты для форматирования текста
├── handlers/              # Обработчики команд и сообщений
│   ├── __init__.py
│   ├── commands.py        # Обработчики команд
│   └── messages.py        # Обработчик текстовых сообщений
├── memory/                # Папка для хранения файлов памяти пользователей (создается автоматически)
├── requirements.txt       # Зависимости проекта
├── sync.sh                # Скрипт синхронизации на сервер
├── .gitignore             # Игнорируемые файлы для Git
└── README.md              # Документация
```

## Особенности

### Фреймворк SMART

Бот помогает формулировать цели по фреймворку SMART:
- **S** - Specific (Конкретная)
- **M** - Measurable (Измеримая)
- **A** - Achievable (Достижимая)
- **R** - Relevant (Релевантная)
- **T** - Time-bound (Ограниченная по времени)

### Долгосрочная память

- Автоматическая саммаризация диалога каждые 10 сообщений
- Сохранение контекста на диск для восстановления после перезапуска
- Автоматическая очистка памяти после формулировки цели

### Поддерживаемые модели OpenAI

- `gpt-4o-mini` (дефолтная, быстрая и экономичная)
- `gpt-4o` (более мощная)
- `gpt-4-turbo`
- `gpt-4`
- `gpt-3.5-turbo`
- `gpt-5`
- `gpt-5-mini`
- `gpt-5-nano`

### Отслеживание стоимости

Бот автоматически рассчитывает и логирует стоимость каждого запроса к OpenAI API на основе текущих цен моделей.

## Примечания

- Длинные ответы автоматически разбиваются на части (лимит Telegram - 4096 символов)
- Все ошибки логируются для отладки
- Память пользователей сохраняется в папке `memory/` в формате JSON
- При переключении модели история диалога автоматически очищается
- Для моделей GPT-5 используется `max_completion_tokens` вместо `max_tokens` и не поддерживается параметр `temperature`

## Лицензия

Этот проект создан в образовательных целях.
