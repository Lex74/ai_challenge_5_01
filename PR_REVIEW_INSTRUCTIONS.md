# Инструкция по использованию CI пайплайна для автоматического ревью PR

## Обзор

Система автоматически анализирует Pull Requests и публикует детальное ревью с использованием:
- **RAG** - для получения контекста из документации и кода проекта
- **MCP Git** - для получения diff и информации о файлах
- **OpenAI API** - для генерации ревью с замечаниями и предложениями

## Требования

1. **GitHub репозиторий** с включенными GitHub Actions
2. **OpenAI API Key** - для генерации ревью
3. **Индекс документации** (опционально, но рекомендуется) - для работы RAG

## Настройка

### 1. Настройка GitHub Secrets

В настройках репозитория (`Settings` → `Secrets and variables` → `Actions`) добавьте:

- `OPENAI_API_KEY` - ваш ключ OpenAI API

> **Примечание:** `GITHUB_TOKEN` автоматически предоставляется GitHub Actions, дополнительная настройка не требуется.
>
> **Важно:** В некоторых организациях/enterprise окружениях права `GITHUB_TOKEN` могут быть ограничены.
> Убедитесь, что для workflow разрешены `pull-requests: write`, `statuses: write` и `checks: write`,
> а также что политика организации не запрещает создание status checks.
>
> **Как проверить права `GITHUB_TOKEN`:**
> 1. В репозитории: `Settings` → `Actions` → `General` → раздел **Workflow permissions**.
> 2. Убедитесь, что выбрано **Read and write permissions**.
> 3. В организациях: `Organization Settings` → `Actions` → `Workflow permissions` и убедитесь, что политики не ограничивают запись.
> 4. Если используется fine-grained token вместо `GITHUB_TOKEN`, добавьте права: `statuses: write`, `checks: write`, `pull-requests: write`.

### 2. Создание индекса документации (рекомендуется)

Для работы RAG необходимо создать индекс документации проекта:

```bash
# Индексация документации проекта (README, API, схемы данных)
python index_project_docs.py

# Или с использованием OpenAI для эмбеддингов
USE_OPENAI_EMBEDDINGS=true python index_project_docs.py
```

Индекс будет сохранен в `document_index/index.json` и должен быть закоммичен в репозиторий.

### 3. Проверка workflow файла

Убедитесь, что файл `.github/workflows/pr-review.yml` существует в репозитории.

## Как это работает

### Автоматический запуск

Workflow автоматически запускается при:
- Создании нового PR (`opened`)
- Обновлении PR (новые коммиты) (`synchronize`)
- Повторном открытии закрытого PR (`reopened`)

### Процесс ревью

1. **Получение информации о PR** - через GitHub API
2. **Получение diff** - через MCP Git (с fallback на GitHub API)
3. **Поиск контекста** - через RAG из документации проекта
4. **Генерация ревью** - через OpenAI API с использованием системного промпта
5. **Анализ на критические проблемы** - автоматический анализ ревью
6. **Создание статуса проверки** - устанавливается статус (success/failure) для блокировки мержа
7. **Создание PR review** - создается review с approve/request changes/comment
8. **Публикация комментария** - детальное ревью публикуется в комментариях PR

### Формат ревью

Ревью включает следующие разделы:

- **📋 Общее резюме** - краткое описание изменений и общая оценка
- **✅ Положительные моменты** - что сделано хорошо
- **⚠️ Критические проблемы** - серьезные проблемы, требующие исправления
- **💡 Предложения по улучшению** - конструктивные предложения
- **📝 Мелкие замечания** - стилистические улучшения
- **🔍 Детальный анализ по файлам** - анализ каждого измененного файла

### Статусы и блокировка мержа

Система автоматически анализирует ревью и устанавливает соответствующие статусы:

- **✅ Success** - если нет критических проблем:
  - Статус проверки: `success` с описанием "Ревью пройдено успешно"
  - PR Review: `APPROVE` (одобрение)
  
- **⚠️ Success с замечаниями** - если есть замечания, но нет критических проблем:
  - Статус проверки: `success` с описанием "Ревью завершено, есть замечания"
  - PR Review: `COMMENT` (комментарий)
  
- **❌ Failure** - если обнаружены критические проблемы:
  - Статус проверки: `failure` с описанием "Найдено N критических проблем(ы)"
  - PR Review: `REQUEST_CHANGES` (требуются изменения)
  - **Блокирует мерж** - если в настройках репозитория включены required status checks

> **Важно:** Для блокировки мержа необходимо настроить required status checks в настройках репозитория (`Settings` → `Branches` → `Branch protection rules`). Добавьте `pr-review/ai-reviewer` в список required status checks.

## Локальное тестирование

Вы можете протестировать скрипт локально перед использованием в CI:

### 1. Настройка переменных окружения

Создайте файл `.env` или экспортируйте переменные:

```bash
export OPENAI_API_KEY="your-openai-api-key"
export GITHUB_TOKEN="your-github-personal-access-token"
export GITHUB_REPOSITORY="owner/repo"  # Опционально
```

### 2. Запуск скрипта

```bash
# Базовое использование
python review_pr.py --pr-number 1 --repo owner/repo

# С указанием base и head коммитов
python review_pr.py \
  --pr-number 1 \
  --repo owner/repo \
  --base main \
  --head feature-branch

# С указанием SHA коммитов
python review_pr.py \
  --pr-number 1 \
  --repo owner/repo \
  --base abc123 \
  --head def456

# Без использования RAG (если индекс не создан)
python review_pr.py \
  --pr-number 1 \
  --repo owner/repo \
  --skip-rag
```

### Параметры командной строки

- `--pr-number` (обязательный) - номер Pull Request
- `--repo` (опционально) - репозиторий в формате `owner/repo`. Если не указан, используется `GITHUB_REPOSITORY` из env
- `--base` (опционально) - base коммит или ветка. По умолчанию используется base из PR
- `--head` (опционально) - head коммит или ветка. По умолчанию используется head из PR
- `--skip-rag` (опционально) - пропустить поиск контекста через RAG

## Примеры использования

### Пример 1: Стандартное использование

```bash
python review_pr.py --pr-number 42 --repo myorg/myrepo
```

### Пример 2: С указанием веток

```bash
python review_pr.py \
  --pr-number 42 \
  --repo myorg/myrepo \
  --base main \
  --head feature/new-feature
```

### Пример 3: Без RAG (если индекс не создан)

```bash
python review_pr.py \
  --pr-number 42 \
  --repo myorg/myrepo \
  --skip-rag
```

### Пример 4: Создание статуса проверки вручную (для отладки)

```python
from github import Github
from review_pr import create_status_check

github = Github("YOUR_TOKEN")
repo_name = "owner/repo"
head_sha = "abcdef123456"

await create_status_check(
    github,
    repo_name,
    head_sha,
    state="success",
    description="Проверка выполнена",
    context="pr-review/ai-reviewer"
)
```

### Пример 5: Создание PR review вручную (для отладки)

```python
from github import Github
from review_pr import create_pr_review

github = Github("YOUR_TOKEN")
repo_name = "owner/repo"
pr_number = 42

await create_pr_review(
    github,
    repo_name,
    pr_number,
    review_text="Тестовое ревью",
    event="COMMENT"
)
```

## Настройка системного промпта

Системный промпт для ревьюера находится в `constants.py` (переменная `PR_REVIEW_SYSTEM_PROMPT`).

Вы можете настроить его под свои нужды, изменив:
- Критерии анализа кода
- Формат вывода
- Приоритеты проверок

## Обработка ошибок

### Если MCP Git недоступен

Скрипт автоматически использует GitHub API как fallback для получения diff.

### Если RAG индекс не найден

Если индекс документации не создан, скрипт продолжит работу без RAG контекста. Используйте флаг `--skip-rag` для явного отключения.

### Если ревью слишком длинное

Ревью автоматически разбивается на несколько комментариев, если превышает лимит GitHub (65536 символов).

## Лимиты и ограничения

- **GitHub Actions**: лимит времени выполнения зависит от плана (20-360 минут)
- **OpenAI API**: лимиты на токены зависят от модели и вашего плана
- **GitHub API**: лимит на размер комментария - 65536 символов (автоматически разбивается)

## Отладка

### Логи в GitHub Actions

Логи доступны в разделе `Actions` → выберите workflow run → `review` job.

### Локальная отладка

Включите подробное логирование:

```python
# В review_pr.py уже настроено логирование на уровне INFO
# Для DEBUG уровня измените:
logging.basicConfig(level=logging.DEBUG)
```

### Проверка MCP Git

Убедитесь, что MCP Git сервер работает:

```bash
# Проверка доступности инструментов
python -c "import asyncio; from mcp_git_client import list_git_tools; print(asyncio.run(list_git_tools()))"
```

## Настройка блокировки мержа

Для блокировки мержа при наличии критических проблем:

1. Перейдите в настройки репозитория: `Settings` → `Branches`
2. Создайте или отредактируйте правило защиты ветки (Branch protection rule)
3. Включите "Require status checks to pass before merging"
4. Добавьте `pr-review/ai-reviewer` в список required status checks
5. Сохраните изменения

Теперь PR с критическими проблемами не сможет быть смержен до исправления проблем.

## Часто задаваемые вопросы

### Q: Ревью не публикуется в PR

**A:** Проверьте:
1. Права доступа workflow (`pull-requests: write`, `statuses: write`, `checks: write`)
2. Наличие `GITHUB_TOKEN` (автоматически предоставляется)
3. Логи в GitHub Actions на наличие ошибок

### Q: Статус проверки не устанавливается

**A:** Убедитесь, что в workflow есть права `statuses: write` и `checks: write` в секции `permissions`.

### Q: PR не блокируется при критических проблемах

**A:** Проверьте:
1. Настроены ли required status checks в настройках репозитория
2. Добавлен ли `pr-review/ai-reviewer` в список required checks
3. Установлен ли статус `failure` (проверьте логи)

### Q: RAG не находит контекст

**A:** Убедитесь, что:
1. Индекс создан: `python index_project_docs.py`
2. Файл `document_index/index.json` закоммичен в репозиторий
3. OLLama доступен (если используется для эмбеддингов)

### Q: Скрипт работает медленно

**A:** Это нормально, так как:
- RAG поиск требует времени
- Генерация ревью через OpenAI может занять несколько секунд
- Для больших PR процесс может занять 1-3 минуты

### Q: Как отключить автоматическое ревью для определенных PR?

**A:** Добавьте проверку в workflow (например, по меткам или названию ветки):

```yaml
- name: Check if review needed
  run: |
    if [[ "${{ github.event.pull_request.title }}" == *"[skip-review]"* ]]; then
      echo "Skipping review"
      exit 0
    fi
```

## Дополнительные возможности

### Настройка модели OpenAI

Измените модель в `review_pr.py`:

```python
from constants import DEFAULT_MODEL
# Используйте другую модель, например:
model = "gpt-4o"  # вместо DEFAULT_MODEL
```

### Настройка параметров RAG

В функции `get_rag_context` можно настроить:
- `top_k` - количество релевантных чанков
- `relevance_threshold` - порог релевантности
- `rerank_method` - метод реранкинга

### Кастомизация системного промпта

Отредактируйте `PR_REVIEW_SYSTEM_PROMPT` в `constants.py` для изменения:
- Критериев анализа
- Формата вывода
- Приоритетов проверок

## Поддержка

При возникновении проблем:
1. Проверьте логи в GitHub Actions
2. Убедитесь, что все зависимости установлены
3. Проверьте наличие необходимых переменных окружения
4. Убедитесь, что индекс документации создан и закоммичен

## Обновление

При обновлении системы:
1. Обновите зависимости: `pip install -r requirements.txt`
2. Обновите индекс документации: `python index_project_docs.py`
3. Проверьте изменения в workflow файле
